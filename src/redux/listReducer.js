import { GET_LISTS } from "../constants"

export default function listReducer(state = {lists: []}, action){
    switch(action.type) {
    //     case value: 
    //     break;

        default: 
            return state
    }

}